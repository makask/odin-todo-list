export default class RenderTaskModal {
  static resetForm() {
    document.querySelector('.add-task-form').reset();
  }
  static resetAddProjectForm() {
    document.querySelector('.add-project-form').reset();
  }

  static resetAddProjectTaskForm() {
    document.querySelector('.add-project-task-form').reset();
  }
}
